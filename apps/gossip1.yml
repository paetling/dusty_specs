repo: github.com/paetling/gossip

image: python:3.5
mount: /gossip

commands:
  once:
    - pip install -r requirements.txt
  always:
    - bash run_gossip.sh

compose:
  environment:
    MY_ADDRESS: gossip1
    MY_PORT: 80
